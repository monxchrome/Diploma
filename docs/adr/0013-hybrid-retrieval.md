# 0013 Hybrid retrieval

Use a versioned Qdrant collection with named dense and sparse vectors. HYBRID requests independently retrieve both candidate sets and use reciprocal-rank fusion before stable evidence selection.
