# 0014 Sparse embeddings

Use an application-scoped, deterministic local sparse provider for development. It supports document and query embedding without external APIs and exposes provider/model/version metadata in point payloads.
