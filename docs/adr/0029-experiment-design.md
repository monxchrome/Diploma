# ADR 0029: Experiment design

Experiment configuration, variants, test cases, repetitions, limits, cost cap, and rubric version are persisted before execution. The worker processes a server-controlled queue and records individual metrics plus aggregate reports. Early Phase 6 metrics are deterministic synthetic fixtures and must remain explicitly marked until a real evaluator is approved.
