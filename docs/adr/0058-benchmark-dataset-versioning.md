# ADR 0058: Benchmark dataset versioning

## Decision

Make dataset versions, cases and evidence packages hash-addressable and immutable after freeze.

## Consequences

Runs can cite their exact evaluation material. Corrections require a new version instead of rewriting prior results.
