# ADR 0032: Container hardening

Use multi-stage images, non-root users, dropped capabilities, read-only filesystems where feasible, health checks, and explicit volumes. Internal services have no host-port mappings.
