# ADR 0056: Role-level model assignment

## Decision

Bind each benchmark agent role to a pinned model profile and prompt version in an immutable variant.

## Consequences

Homogeneous, heterogeneous and independent-critic variants can be compared without accepting arbitrary browser-supplied models or prompts.
