# Dataset Design

The built-in synthetic dataset contains bounded, non-production decision cases with scenario, question, objectives, constraints, assumptions, reference facts, risks, alternatives and known unknowns. Dataset versions receive a content hash and cannot be edited after freeze. Controlled evidence is stored as immutable evidence packages with citation mappings.

Sensitive or private source material must remain project-scoped and follows existing authorization and export policy.
