# Local Qwen Through Ollama

Qwen is stored as the `family` of a trusted profile. Ollama is stored as its `LOCAL_OLLAMA` runtime and provider integration, never as the model family. Local comparison requires exact model ID, digest where available, parameter size, quantization, context window and hardware profile. Results cannot be directly generalized to different hardware or quantization.
