# LLM as Judge

LLM judging is optional and versioned through a judge policy, profile and prompt version. Candidate order must be blinded and randomized; the judge should not see variant labels, provider secrets or hidden reasoning. Judge outputs are measurements with known bias, not ground truth. A second judge and disagreement reporting are preferred when available.
