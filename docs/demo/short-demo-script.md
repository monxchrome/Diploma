# Short Demo Script (3 Minutes)

1. State that the scenario is synthetic and that the platform is decision support, not automated truth.
2. Show the synthetic source pack and explain project-scoped evidence.
3. Show the architecture and explain that the browser talks only to the API, which calls the AI service.
4. Explain grounded citations, quality gates, report snapshots, and controlled external research boundaries.
5. Show the safe `/api/version` payload and release audit; disclose that the full seeded application demo is still a release blocker.

This script is valid without internet and does not claim a live run or benchmark result.
