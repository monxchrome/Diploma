# Main Demo Script (Requires Verified Seed)

Use this script only after the demo seed/reset blocker is closed in a separate verified change.

1. Introduce the decision question and the synthetic-only data boundary.
2. Log in with a non-production demo account and open the synthetic project.
3. Show source readiness, source excerpts, and the rejected prompt-injection case.
4. Run or open a pre-generated grounded result; identify E-citations and uncertainty/conflict indicators.
5. Compare Focused and Multi-perspective modes without claiming a universal winner.
6. Show decision readiness, immutable report version, private export, and revoked-share behaviour.
7. Show fake billing/entitlements and a controlled benchmark fixture only when clearly labelled synthetic.
8. Disconnect external access and use the offline fallback if a provider is unavailable.

Current status: **not executable in this working tree** because the application seed/reset/report fixture is missing.
